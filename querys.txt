ALTER TABLE inf_diario_ultimos_dias
ADD COLUMN dia INT,
ADD COLUMN mes INT,
ADD COLUMN ano INT;

UPDATE inf_diario_ultimos_dias
SET dia = EXTRACT(DAY FROM dt_comptc)::INT,
    mes = EXTRACT(MONTH FROM dt_comptc)::INT,
    ano = EXTRACT(YEAR FROM dt_comptc)::INT;