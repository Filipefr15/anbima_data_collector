
select inf.cnpj_fundo_classe AS cnpj,
		inf.vl_patrim_liq,
		reg.denominacao_social
		FROM registro_fundo reg
		JOIN inf_diario_ultimos_dias inf
		ON inf.cnpj_fundo_classe = reg.cnpj_fundo
		order by inf.vl_patrim_liq desc
